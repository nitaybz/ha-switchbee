"""Shared pytest fixtures.

Phase 0 keeps this empty so the test suite stays runnable without
Home Assistant installed. Phase 1+ will introduce HA-shaped fixtures
(see plan task 1.1 onward) once `pytest-homeassistant-custom-component`
is pinned to a Python 3.12 / HA 2024.1.0 compatible version.
"""
